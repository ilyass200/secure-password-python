# Simulation de la passerelle du cloud externe
class ExternalGateway:
    def save_password(password):
        print(password)
        if password:
            return True
        else:
            return False